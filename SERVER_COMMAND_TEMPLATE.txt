sudo bash -lc '
set -e
REPO="https://github.com/alafyothman5-gif/Price-bot.git"
TMP="/root/pricebot_clean_openrouter_deploy"
apt-get update -qq
DEBIAN_FRONTEND=noninteractive apt-get install -y git >/dev/null
rm -rf "$TMP"
git clone "$REPO" "$TMP"
DEPLOY_FILE=$(find "$TMP" -maxdepth 4 -name "DEPLOY_FROM_GITHUB.sh" | head -1)
cd "$(dirname "$DEPLOY_FILE")"
chmod +x DEPLOY_FROM_GITHUB.sh
bash DEPLOY_FROM_GITHUB.sh
'
